<%= form_for @link_nature, :html => { :class => "form-horizontal link_nature" } do |f| %>

    <% if @link_nature.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@link_nature.errors.count, "error") %> prohibited this link_nature from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @link_nature.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%= error_span(@link_nature[:name]) %>
  </div>
  <div class="control-group">
    <%= f.label :description, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_area :description, :class => 'form-control' %>
    </div>
    <%= error_span(@link_nature[:description]) %>
  </div>
  <div class="control-group">
    <%= f.label :color, :class => 'control-label' %>
    <div class="controls">
      <%= f.color_field :color, :class => 'form-control' %>
    </div>
    <%= error_span(@link_nature[:color]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            link_natures_path, :class => 'btn btn-default' %>

<% end %>
